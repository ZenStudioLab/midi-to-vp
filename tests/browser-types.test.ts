import { describe, expect, it } from 'vitest';

describe('browser type fixture placeholder', () => {
	it('delegates browser-only type coverage to browser-types-check.ts', () => {
		expect(true).toBe(true);
	});
});